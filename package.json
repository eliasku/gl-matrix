{
  "name": "ts-gl-matrix",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "format": "prettier --write **/*.{ts,json,md}",
    "check": "tsc -p .",
    "lint": "eslint .",
    "build": "tsup ./src/index.ts --target esnext --format esm --minify terser --dts --sourcemap"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "optionalDependencies": {
    "prettier": "latest",
    "eslint": "latest",
    "typescript-eslint": "latest",
    "typescript": "latest",
    "tsdown": "latest",
    "tsup": "latest",
    "terser": "latest"
  }
}
