{
  "name": "ts-gl-matrix",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "format": "prettier --write **/*.{ts,json,md}"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "optionalDependencies": {
    "prettier": "latest",
    "eslint": "latest"
  }
}
